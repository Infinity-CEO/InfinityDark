using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TakeDamage : MonoBehaviour
{
    /*public enum collsionType
    {
        head, body, Larams, Llegs, Rarams, Rlegs
    }
    public collsionType damageType;
    public EnemyFSM enemyHp;
    public void Hit(float value)
    {
        try
        {
            enemyHp.hp -= value;
            if(enemyHp.hp <= 0)
            {
                enemyHp.Die();
            }
        }
        catch
        {
            print("controller is not connected");
        }
    }*/
}